# 1.0

皆さん、0.25ベータを宣言してから約1年が経ちました。その間は、不具合の修正とプロジェクトに必要な機能の完成へ主に取り組んできました。

1.0は以前から念頭にありました。より強固なCI、詳細なテスト、同期に適したE2Eランナー、実機向けテストツールを整備し、個別の道具ではなく一貫した基盤としてまとめました。ようやく、リポジトリの構造を作り直す時が来たように思います。

Issue報告、プルリクエスト、スポンサーシップ、そしてOpenAI Codex for Open Sourceによる支援がなければ実現できませんでした。改めて感謝します。プロジェクトへ寄せられたすべてのプルリクエストと同様に、Codexなどで作られたコードも、vrtmrz本人が確認・監査しています。

今後のプロジェクトのために、より健全な基盤を築く上で、引き続きご協力いただければ幸いです。

以前のリリースは、1.0のリリース履歴、1.0プレビューのリリース履歴、0.25のリリース履歴、および従来のリリース履歴から確認できます。

## 未リリース

## 1.0.18

2026年8月24日

### 同期とストレージ

#### 修正

- リセットおよび再構築の処理で、更新後の設定が選択するローカルデータベースを使用するようになりました。**データベース接尾辞**を変更した後に古いデータが再度開かれることを防ぎます。データベースの初期化が完了しない場合は、不完全な状態で続行せず、処理を停止します。

#### 改善

- 再構築時に、復元されたファイルイベントを現在のVaultに対して再確認し、現在のファイル内容を使用して、プラグインが準備完了を報告する前に処理を完了するようになりました。

### 日本語版での改善

- P2Pの**メッセージサイズ**と**接続経路**の設定名、およびそれぞれの説明を日本語化しました。既存の翻訳カタログを利用しており、日本語版固有のキーは追加していません。

## 1.0.17

2026年8月23日

### インターフェースと翻訳

#### 修正

- 設定マニフェストから生成する設定、セットアップウィザードの設定概要、および外部で変更された設定に関する警告で、翻訳がある場合は**表示言語**を反映するようになりました（PR #1123）。@nimula に感謝します。

### P2P同期

#### 改善

- P2P接続プロファイルに、4種類の**P2Pメッセージサイズ**プリセットと、**自動**または**TURNリレーのみ**を選択する**接続経路**を追加しました。大きなWebRTCメッセージが分割または破棄される経路では、小さなメッセージで互換性が改善する場合があります。TURNリレーのみを使用するには、TURNサーバーの設定が必要です。P2P接続文字列と暗号化されたセットアップURIには、両方の選択が保持されます。
  - vrtmrz/livesync-commonlib#97で800バイトのしきい値を含む詳細な断片化診断を提供してくださった @andrewschreiber に感謝します。この互換性設計の参考になりました。
- TURNリレーが必要なP2P構成向けに、任意のセルフホスト型Coturn Composeスターターを追加しました。固定したupstreamイメージを使用し、ネットワーク、認証情報、セキュリティ、および検証時の注意点を説明しています。

### 日本語版での改善

- 新しいP2P接続互換性の見出し、選択肢、説明、およびTURN設定不足時の通知を日本語化しました。独自の翻訳キーは追加していません。

## 1.0.16

2026年8月19日

### 競合処理と復旧

#### 修正

- Document Historyの「このリビジョンへ戻す」は、選択した内容を削除されていない新しい後続リビジョンとして作成してからVaultへ反映するようになりました。論理削除の後で読み取り可能なリビジョンを復元しても、後続の同期で削除により上書きされません。
  - 復元中にファイルが変更された場合は、古いリビジョンを元に処理を続けず停止します。既存の競合は「競合およびファイル／データベースの差異を点検」で引き続き確認できます。

### 同期とストレージ

#### 改善

- 1回限りのCouchDB同期では、複製の開始前に停止したWeb互換接続確認を解放するようになりました。後続の同期で新たに接続を試行できます（Commonlib 0.1.16）。
  - 60秒の保護は複製開始前の接続確認だけに適用され、通常の同期を制限しません。「内部APIを使用」の経路も変わりません。

### インターフェースと翻訳

#### 改善

- Document Historyでの復元に関する失敗、競合、保存、Vault反映、および完了通知を日本語化しました。

## 1.0.15

2026年8月15日

### 同期とストレージ

#### 改善

- 起動時のオフラインスキャンを高速化しました。特にパス難読化を使用する大きなVaultで効果があります（Commonlib 0.1.15）。

### インターフェースと翻訳

#### 改善

- 上流では繁体字中国語の翻訳カタログを完成させ、用語と表現の自然さを改善しました（PR #1106）。貢献してくださった @nimula に感謝します。
- この版では日本語カタログの新しいキーや直接表示されるUI文言は追加されていません。既存の日本語表示を維持しました。

## 1.0.14

2026年8月14日

### 同期とストレージ

#### 修正

- 完了するCouchDB操作は、CommonlibとLiveSyncの両方で使用後に一時的なリモートデータベース接続を閉じるようになりました。対象には、セットアップウィザードと設定画面の接続確認、コマンドラインのマイルストーン検証、データベースメンテナンス、Security Seedの更新、状態照会、再試行、およびエラー経路が含まれます（Commonlib PR #112）。
  - これは、長時間にわたるリソース増加に寄与する可能性があるとして #1034 で追跡されている、現時点で確認済みの一時接続すべてを対象とします。長時間セッションでの検証は継続中です。
  - 貢献してくださった @apple-ouyang に感謝します。

### インターフェースと翻訳

#### 改善

- この版では翻訳カタログおよび直接表示されるUI文言の追加はありません。既存の日本語表示を維持しました。

## 1.0.13

2026年8月13日

### 競合処理と復旧

#### 改善

- 「競合およびファイル／データベースの差異を点検」は、記録されたパスから導出されるIDと保存済みドキュメントIDが一致しないローカルメタデータを報告するようになりました。通常のスキャンは未解決の項目と対応するVaultパスを変更せずに残し、同じ論理パスに対して一貫したIDを持つメタデータは通常どおり処理します。
  - 競合のない有効な項目に明確な移動先が1つだけある場合は、確認後にレンチメニューからそのローカルメタデータドキュメントを修復できます。移動元IDを削除する前に移動先を書き込み、検証します。曖昧な項目や安全に扱えない項目は読み取り専用のままです。

### インターフェースと翻訳

#### 改善

- Metadata IDの点検、修復、確認ダイアログ、および結果通知を日本語化しました。

## 1.0.12

2026年8月11日

### 同期とストレージ

#### 修正

- 1回限りのCouchDB同期の実行後と再試行前に、一時的なリモートデータベースを閉じるようにしました。長時間の定期同期で非アクティブなPouchDBインスタンスが蓄積することを防ぎます（Commonlib PR #75）。@apple-ouyang に感謝します。
- 起動時および復旧時のスキャンで、データベースからVaultへの書き込みに失敗した場合でも、成功として記録せず再試行可能な状態を保つようにしました。サイズ上限を超えるファイルと競合中のファイルは引き続き意図的にスキップし、実際の書き込み失敗はFast Setup、CLIミラー、およびデーモン呼び出し元へ報告されます（Commonlib PR #106）。

### インターフェースと翻訳

#### 改善

- この版では翻訳カタログおよび直接表示されるUI文言の追加はありません。既存の日本語表示を維持しました。

## 1.0.11

2026年8月9日

### セットアップと互換性

#### 修正

- CouchDBの「内部APIを使用」を有効にしている場合、Fast Setupはバッファリング型の内部APIで利用できないストリーミング取得を避け、標準取得を使用するようにしました（Issue #1020）。カスタムヘッダーのみを使用する場合は、ブラウザーのCORSが許可されれば引き続きFast Fetchを使用します。標準取得ではローカルデータベースのリセット後に古いFast Fetchチェックポイントを消去します。

### 同期とストレージ

#### 修正

- 小数を含むファイル更新日時が、同期後に一部のモバイル端末でクラッシュを引き起こさないようにしました（Issue #1087、PR #1039）。コマンドラインツールおよびObsidianのネイティブファイルシステムへの書き込み前に、更新日時を正規化します。@andrewleech に感謝します。

### インターフェースと翻訳

#### 改善

- この版では翻訳カタログおよび直接表示されるUI文言の追加はありません。既存の日本語表示を維持しました。

## 1.0.10

2026年8月9日

### セットアップと互換性

#### 修正

- Fast SetupでCouchDBの変更フィードを取得するすべてのリクエストに、設定済みのカスタムヘッダーを送信するようにしました。Cloudflare Accessなどのリバースプロキシでも、通常の同期と同じ認証で初期セットアップできます（Commonlib PR #82）。@nimula に感謝します。

### インターフェースと翻訳

#### 改善

- この版では翻訳カタログおよび直接表示されるUI文言の追加はありません。既存の日本語表示を維持しました。

## 1.0.9

2026年8月8日

### セットアップと互換性

#### 修正

- 複数分割された設定QRコードで、パスワード、パスフレーズ、その他の設定に含まれる特殊文字を保持するようにしました（PR #1083）。@calvinbui に感謝します。
- Fast Setupは、1行の状態確認で有限のCouchDB変更ページのサイズを決定し、取得結果と`pending`を合わせて数えるようにしました。不透明な`last_seq`をトークン表現の比較なしに次ページへ引き継ぎます。各ページはハートビートではなく1秒のアイドルタイムアウトを使用するため、CouchDB 3.2でも現在利用可能な行の保存後に終端を返せます。

### インターフェースと翻訳

#### 改善

- Fast Setupの既存の選択肢と、キャンセル後に安全な停止状態を保つ案内の日本語表示を維持しました。

## 1.0.7

2026年8月8日

### セットアップと互換性

#### 修正

- Fast Setupは、取得したCouchDB変更位置が保存されてから完了するようになりました。復号、プロトコル、またはローカルへの書き込みに失敗した場合、不完全なデータベースを完了状態にせずに停止します。一時的な中断では、最後に確実に保存した位置から再開します（#1065）。

### インターフェースと翻訳

#### 改善

- Fast Setupの選択肢と、失敗時にVaultおよびデータベースの反映を安全に停止する挙動の日本語表示を維持しました。

## 1.0.6

2026年8月6日

### セットアップと互換性

#### 修正

- 初期セットアップ時に、保存済みの同期設定がない新しいリモートと、リモート設定の読み取り失敗を区別するようにしました。新しいリモートではこのデバイスの設定を使用できます。読み取り不能な場合、Fetchは停止し、Rebuildではこのデバイスの設定による上書きを明示的に続行できます。キャンセル時は選択済みの自動同期モードを維持し、Vaultとデータベースの反映を一時停止して再起動します（#1064）。

### インターフェースと翻訳

#### 改善

- 新しいリモート設定の案内、再試行、キャンセル、およびRebuildの上書き確認を日本語化しました。
- 設定差分ダイアログとFast Setupの選択肢の日本語表示を維持しました。

## 1.0.5

2026年8月5日

### 同期とストレージ

#### 改善

- 有限同期操作中に画面のスリープを抑止するかを設定できるようにしました。デスクトップでは既定で自動スリープを許可し、モバイルでは一般設定を有効にしない限り画面スリープ抑止を維持します（#1073）。

### インターフェースと翻訳

#### 改善

- 同期中のスリープ設定を日本語化しました。
- 韓国語翻訳が、セットアップ、P2P、リモート設定、診断、およびメンテナンスを含む現在のカタログ全体をカバーするようになりました（PR #1075）。

#### 修正

- iOSのエディター内LiveSyncステータスがビュー見出しの操作部と重ならないようにしました（PR #1067）。

## 1.0.4

2026年8月5日

### 同期とストレージ

#### 修正

- 新しいリモート設定の接続テストまたは保存時に、レプリケーターの構築中にローカルデータベースへアクセスしないようにしました。ローカルデータベースの初期化前に発生する「ローカルデータベースはまだ準備できていません」エラーを防ぎます（#1064）。

### コマンドラインツール

#### 修正

- セットアップおよびリモート設定コマンドが成功した場合、設定変更を保持するようにしました。ほかのコマンドでは、`--write-settings` を指定しない限り設定ファイルを変更せず、一時的なCLI停止値も書き込みません（#1070）。

## 1.0.3

2026年8月3日

### 同期とストレージ

#### 修正

- 現在のVault内容が既知の同期履歴と一致している場合、古いリビジョンを追加で読み取らないようにしました。これにより、読み取れない古いリビジョンによる不要な「ドキュメント内容が見つかりません」警告を防ぎます。
- 同じリクエストで一部のチャンクを取得できなくても、正常に取得できたチャンクを保持して処理できるようにしました（#771）。

### インターフェース

#### 修正

- 修復設定で設定済みのファイル更新日時上限を表示する際に `HierarchyRequestError` が発生しないようにしました。

## 1.0.2

2026年7月31日

### 同期とストレージ

#### 改善

- 同期済み文書のバッチでは、キューに入ったすべてのファイルのローカルストレージへの反映が完了するまで、画面のスリープ抑止とライフサイクル保護を最善努力で維持します。リモートアクティビティ表示の継続時間は変わりません。

#### 修正

- Vaultへ取り込む際に先頭のUTF-8バイトオーダーマークを保持し、ファイルメタデータとの内容サイズ不一致と、継続する3バイトの整合性不一致を防ぎます。

### インターフェースと翻訳

#### 改善

- 文書履歴で前後のリビジョンへ移動でき、現在のリビジョン位置を表示します。最も古い／新しいリビジョンでは移動操作を無効化し、検索結果の移動動作は変えません。
- セットアップ、P2P、カスタマイズ同期、グローバル履歴、JSON競合処理、およびリモート設定の残りのUI文言を翻訳カタログ経由で表示します。
- 日本語版では、新たにカタログ化された設定、セットアップ、P2P、カスタマイズ同期、履歴、および競合処理のUI文言を日本語化しました。

### コマンドラインツール

#### 修正

- Node.jsで`FileReader`が利用できない場合、大きなバッファのBase64エンコードに公開済みの`octagonal-wheels`フォールバックを使用し、スライスされたバイナリビューも正しく処理します。

## 1.0.1

2026年7月29日

実験的なWebApp、WebPeer、および共有ダイアログ構成の堅牢性と保守性を改善したメンテナンスリリースです。通常のObsidianプラグイン利用には大きな変更はありません。

### インターフェース

#### 改善

- オンボーディング通知の独自配置を廃止し、Obsidian標準の表示位置と閉じ方に従うようにしました。
- WebAppでは、自動ファイル監視を利用できない場合に、固定のブラウザバージョン案内ではなく**ローカルファイルをスキャン**する操作を案内します。
- WebApp／WebPeerのP2Pメニュー、TURN設定、Vault選択、状態表示、および確認ダイアログを日本語化しました。

## 1.0.0

2026年7月27日

1.0に向けた作業が非常に大きくなったため、[解説記事](https://fancy-syncing.vrtmrz.net/blog/0036-livesync-1_0_0-en.html)を公開しました。

### セットアップと互換性

#### 改善

- 未設定のVaultは、ユーザーが開始するまでセットアップを待つようになりました。オンボーディングは継続表示される通知から開始でき、**Self-hosted LiveSync設定** → **セットアップ**からも再実行できます。
- セットアップ時に、名前付きのCouchDB、オブジェクトストレージ、およびP2P接続を作成するようになりました。Setup URIは接続名と選択状態を保持し、通常の起動スキャンより前に取得または再構築を予約します。
- CouchDBの手動セットアップでは、最初のデータベースを作成する場合と、別のデバイスを接続する場合を区別します。オンボーディングでは接続成功が必須です。設定画面では未検証の接続を明示的に保存でき、サーバー設定の修正候補を個別に確認できます。
- チャンクのハッシュアルゴリズム、チャンクサイズ、または分割バージョンだけの互換性のある差異は、既定で自動調整されます。既存チャンクは引き続き読み取れます。自動調整を無効化することもでき、互換性のない設定を含む差異は従来どおり手動確認が必要です。

#### 修正

- 古いリリースでファイル名の大文字小文字設定が明示されていなかった場合の互換動作を含め、既存Vaultの有効な従来設定を維持するよう修正しました。

#### セキュリティ

- Fly.ioセットアップで、CouchDBとVault暗号化の秘密情報を暗号学的に安全な乱数から生成するようにしました。
- 細工されたパスパターンと`mailto:`リンクによる過剰なCPU使用に対処する依存関係更新を含みます。

### 競合処理と復旧

#### 改善

- **今はしない**を選ぶと、未解決の競合警告を残したまま、自動マージダイアログの繰り返しを延期できます。有効なリビジョンが3つ以上ある場合は、再現可能な組み合わせで2つずつ確認し、解決済みの組み合わせは再起動後も維持されます。明示的なコマンドから延期した競合を再度開けます。
- **競合とファイル／データベースの差異を調査**では、Vaultとデータベースの採用リビジョン、およびすべての有効な競合リビジョンを比較します。不足チャンク、`Δsize`、`Δtime`、Vaultが採用リビジョンと一致するか、競合が残っているかをコンパクトに表示します。
- 報告された各ファイルと有効なリビジョンにはレンチメニューがあり、比較、読み取れる対象リビジョンの適用、バイト単位の一致記録、選択ブランチの子としてVault内容を保存、ツリーを変えずに不足チャンク取得を再試行、または選択した有効ブランチの明示的な破棄を行えます。

#### 修正

- テキストと構造化データの自動マージで、両ブランチが実際に共有する最も近いリビジョンを使用するようになりました。別のデバイスで解決した競合が、Vaultに削除済みブランチと同じ内容が残っているだけで再作成される問題を修正しました。
- ファイルが競合中に行った編集、論理削除、および名前変更は、そのデバイスに表示されたリビジョンを延長します。関係を証明できない場合、LiveSyncは確認のためブランチを保持します。
- 読み取れない有効なリビジョンを自動処理中も保持します。Vaultファイルがなく、採用リビジョンが論理削除の場合は、別の有効ブランチがない限り一致として扱います。
- ガベージコレクションV3はCouchDB専用になり、すべての有効な競合ブランチ、必要な共通祖先、および共有チャンクを保護します。デバイス進捗を確認できない場合は停止し、圧縮失敗を成功と同時に報告しないよう修正しました。

### P2Pと任意の同期機能

#### 改善

- P2Pと隠しファイル同期は、任意で有効化するサポート対象機能です。カスタマイズ同期は上級者向けのサポート対象機能です。データ圧縮も利用できますが、既定では無効です。
- P2Pを設定するまで、通常のCouchDB利用画面にP2P操作を表示しません。現在の状態ペインでは、変更の通知、ピアの追跡、およびデバイスごとの永続操作を区別します。
- P2Pセットアップと案内で、必須のシグナリングリレーと任意のTURNを区別し、置き換え可能な公開リレーのプライバシーおよび可用性上の制限を説明するようにしました。
- 隠しファイル同期を有効化すると、設定保存前に1つの進捗通知を開き、初回スキャン完了まで再利用します。段階ごと、再読み込み、再起動の通知が重複しなくなりました。

#### 修正

- 最初のデバイスのP2Pセットアップは、別のピアがオンラインでなくてもシグナリングテストを完了できます。追加デバイスでの取得には、引き続き利用可能な取得元ピアと、完了済みのP2P再構築が必要です。
- 設定変更とデータベースリセット後に、P2Pリレー接続を確実に閉じ、再作成するよう修正しました。

### インターフェース、翻訳、および運用

#### 改善

- コマンドパレットの操作名を明確にし、その機能と現在の状況で使用できる場合だけ表示します。既存のホットキーを維持するため、名前を変更したコマンドのIDは保持します。
- セットアップと確認ダイアログのテキストを、コピーまたは翻訳のために選択できるようにしました。
- リモート容量警告を、継続表示されるクリック可能な通知に変更しました。初回アップロードと再構築では、事前にすべてのチャンクを送信する確認を行わず、通常のレプリケーションで転送を完了します。
- プラグイン独自のゴミ箱設定と固定チャンクリビジョンの廃止済み操作を削除しました。変更履歴は引き続き利用できますが、自動では開かず、未読数も管理しません。
- Self-hosted LiveSyncが自身の翻訳カタログを管理するようになりました。Commonlibはほかの利用側へ正規の英語を提供し、翻訳はSelf-hosted LiveSync本体リポジトリへ貢献できます。

#### 修正

- 利用可能なUI翻訳の適用確認が、要求していないダイアログで起動を止めないよう修正しました。継続表示される通知から、既存の詳細画面を必要に応じて開けます。
- 狭いモバイル画面に合わせて操作ボタンを配置し、長いダイアログでも操作部へ到達できるようにしました。継続通知が閉じる操作を覆う問題も修正しました。

### ストレージとファイル選択

#### 修正

- オブジェクトストレージの任意機能であるカスタムHTTPハンドラーが、バイナリ要求本文から正しいバイト範囲を送信し、未対応の本文形式を空の要求として送らず報告するよう修正しました。
- セレクター、除外規則、サイズ・更新日時上限、またはファイル名の大文字小文字設定を緩和した場合に、リモートでの追加更新を待たず、以前に受信したファイルを再確認します。
- 起動時と全体調査のスキャンで、Vaultとローカルデータベースを比較する前に、組み込みの旧LiveSyncログと復旧フラグファイルを除外します。既存の除外済みデータベース記録とユーザー設定の除外動作は変わりません。

### コマンドラインツール

#### 修正

- CLIのSetup URI検証で、Commonlibのサポート対象ESMパッケージインターフェースを使用するようにしました。
- 非root Dockerイメージが、ソースチェックアウトから継承した権限に依存しないよう修正しました。

#### セキュリティ

- CLIは、Vault操作前にパストラバーサルとシンボリックリンク要素を検出して拒否します。

### 検証

#### テスト

- アップグレード、2デバイス同期、CouchDB、オブジェクトストレージ、P2P、隠しファイル同期、モバイルダイアログ、競合とリビジョン復旧、障害診断、および厳格な後片付けについて、実際のObsidianを使う自動テストを拡充しました。
- 実際のCouchDB統合テストで、論理削除、共有・競合チャンクの保持、圧縮、下流レプリケーション、および内容アドレス方式チャンクの再作成を検証しました。
- 暗号化した実Obsidian再接続シナリオで、片方のクライアントが以前の値を持つ状態からリモートのセキュリティシードを置き換え、古い値を復元せずに新しい値を採用することと、双方向の暗号化往復を検証しました。
- このリリースのプラグインをBRATからインストールし、macOS、iOS、およびAndroidで検証しました。0.25.83からの更新、双方向同期、P2Pセットアップ、競合処理、復旧操作、モバイルレイアウト、および既存設定での起動を含みます。
- ネイティブ版と非root Docker版CLIで、セットアップ、書き込み、読み取り、一覧、情報、削除、競合解決、およびリビジョン取得を検証しました。

# 0.25
2025年7月19日以降（0.25.0-beta1 は 2025年7月13日）

0.25 の冒頭ノートは [updates_old.md](https://github.com/vrtmrz/obsidian-livesync/blob/main/updates_old.md) に移動しました。0.25 では多くの更新が入りましたが、幸い互換性は維持されており、破壊的変更は不要でした。十分に安定すれば、次のバージョンは v1.0.0 になる見込みです。あくまで希望ではあります。

## 0.25.83

2026年7月16日

皆様の多大な貢献により、プラグインは日々改善し続けています。2023年に初めて報告された問題がついに解決されました。ご協力いただいたすべてに感謝いたします。

### 修正

- CouchDBのリクエストが完了した後もリモートアクティビティインジケーター 📲 が表示されたままになる問題を修正しました。
- 実行中のオンデマンドフェッチや有限レプリケーションによってまだチャンクが配信される可能性があるにもかかわらず、不足しているチャンクが「利用不可」と誤って報告される問題を修正しました。読み取り処理は実際の配信ライフサイクルに従い、配信完了時にローカルデータベースを再確認するようになります。
- 「ファイルを大文字小文字区別として扱う（Handle files as Case-Sensitive）」がオフの場合に、同じディレクトリ内でファイル名の大文字小文字のみを変更すると、他のデバイスでそのファイルが削除されてしまう問題を修正しました。なお、ディレクトリ名の大文字小文字の変更は引き続きサポート対象外です (#198, PR #1014; [commonlib PR #68](https://github.com/vrtmrz/livesync-commonlib/pull/68))。修正してくださった @metrovoc さんに感謝します！
- **「すべての競合ファイルを新しいもので解決（Resolve All conflicted files by the newer one）」** の実行時に、解決されたファイルごとに個別の成功通知が表示され、確認されたファイル10個中9個で進捗通知が更新されてしまう問題を修正しました。ファイルごとの成功結果はログに出力されるようになり、進捗状況の更新は10ファイルごとに行われ、エラーおよび一括処理の完了通知は引き続き画面に表示されます (#1016, PR #1017)。修正してくださった @apple-ouyang さんに感謝します！

### 改善

- ステータスバーのリモートアクティビティ表示を、有限のリモート操作を示す `📲` と、現在処理中の CouchDB またはオブジェクトストレージリクエストの概数を表す `🌐N` に分割しました。

## 0.25.82

2026年7月15日

最近、Fancy Kit というリポジトリを作成し、その周辺に適切なインフラを構築しようとしています。これが面倒な官僚主義のように見えますか？心配はいりません。通常通りに進めることができます。Codex は、私のいつもの散漫な文章、ひどいプルリクエスト、バラバラな発言をただ整理しているだけです。

### 修正

- 各レプリケーション（同期）の実行前にリモートのセキュリティシード（Security Seed）を更新するようにし、リモートデータベースの再構築中にクライアントを開いたままにしていた場合に、古いシードで暗号化されたデータがアップロードされる問題を修正しました (#1018)。
- P2P の **同期を開始して閉じる** アクションにおいて、同期処理が落ち着くまで待ってからダイアログを閉じるように変更し、同期処理の実行中に画面のスリープ防止（Wake Lock）保護が時期尚早に解除されるのを防止しました。

### 改善

- サポートされているモバイルおよびデスクトップデバイスにおいて、一発（単発）レプリケーション、P2Pのピア検出と選択、データベースの再構築とフェッチ操作、およびリモートチャンクの取得中に、処理が完了するまでの間画面をスリープ状態にしない（スリープ防止）ように改善しました。また、プラットフォームの制限によりバックグラウンド処理が一時停止または強制終了される可能性はありますが、処理が完了するまで LiveSync の通常のアクティブ（表示状態）サスペンド処理を延期するようにしました。
- ステータスバーのインジケーター 📲 が、HTTPリクエストだけでなく、有限のリモート処理もカバーするよう改善しました。正確な物理的接続数ではなく、P2Pのピア選択やリモートチャンクの取得といった広範なリモートアクティビティを報告するようになります。

### 改善（CLI）

- CLIのコンテナイメージが、AMD64 と ARM64 の両方のプラットフォーム向けに公開されるようになりました。

### テスト

- LiveSync CLI を介して暗号化・パス難読化されたノートを書き込み、プラグインが同じコンテンツを実体化（復元）できることを検証する、ローカルでの実Obsidian互換性テストを追加しました。

## 0.25.81

2026年7月14日

次の広範なリリースより前に、チャンク境界の問題の修正を適用できるようにするため、このアップデートをリリースします。問題の再現、トレース、および検証にご協力いただいた皆様に感謝いたします。

### 修正

- Rabin–Karpのチャンク境界に U+FEFF 文字（BOM）が存在する場合にその文字が失われ、再構成されたファイルが変更されてサイズ不一致の競合が繰り返し発生する問題を修正しました (#1000)。

### 改善

- コンパイル済みの無視パターン（ignore patterns）を再利用することで、多数のファイルや無視ルールを持つ Vault での処理オーバーヘッドを削減し、Vault のスキャンおよび CLI のファイルフィルタリング処理を改善しました (#1006, #1007, #1008)。

### 改善（CLI および Webアプリ）

- ルート化されたストレージアダプターにおいて、絶対パス、ドライブ指定パス、バックスラッシュ区切りパス、およびトラバーサルパス（パス移動）を拒否するよう変更しました。また、設定されたルート自体を対象としたファイルの書き込み、追加、および削除を防止するよう制限を追加しました。
- File System Access API ストレージにおいて、既存の Node の挙動と同様に、存在しない親ディレクトリ配下にファイルを新規作成できるよう改善しました。

### テスト

- メタデータ、テキストおよびバイナリ操作、追加（append）、リスト表示、削除、パス内包判定、および空のルート処理に関する、共有された Node および File System Access API ストレージのテストカバレッジを追加しました。
- ローカルでのパフォーマンス調査用として、Compose を使用した P2P エンドツーエンドの煙幕（smoke）テストと再現可能なネットワークベンチマークケースを追加しました。

### その他

- 既存の実行時動作を変更することなく、内部ストレージアダプターのコントラクトを機能別の明確なビューに分割しました。

## 0.25.80

2026年7月7日

### 修正

- マークダウンの競合自動マージを改善し、重複しない編集は自動マージされる一方、削除と編集が重なるような競合箇所は手動解決のために明示的に残るようにしました (#993)。
  - 挙動の変更:
    - 一方が変更されていない行を削除し、もう一方が別の箇所を編集した場合、削除された行がマージ結果に誤って再導入されないようになりました。
    - 一方が行を削除し、もう一方が同じ行を変更した場合、一方を暗黙的に選択するのではなく、競合として保存されるようになりました。
- 新しいデータベースエントリーをストレージに適用する際、古いローカルファイルが誤って競合として保存されることがある問題を修正しました (#994)。
  - 挙動の変更:
    - ローカルストレージにリビジョン履歴に反映されていない未同期の変更が含まれている可能性がある場合、競合として保存されます。新しい受信テキストエントリーが既存のローカルテキストを明らかに拡張している場合に限り、競合を作成せずに適用されます。
- 非表示ファイルの同期（Hidden File Sync）で「無効化して上書き（Disable and then Overwrite）」を選択した際、非表示ファイルの同期が無効化されたまま上書きセットアップが実行され、非表示ファイルが暗黙的にスキップされる問題を修正しました (#989, PR #992)。
  - 非表示ファイルの同期は、初期化（Fetch、Overwrite、Mergeなど）の完了後ではなく、開始前に再有効化されるようになりました。初期化が失敗した場合、設定は有効のまま残ることがあります。

## 0.25.79

2026年6月29日

### 修正

- 高速同期（Fast Fetch）で一時的なストリーム切断などの一般的なネットワーク切断やプラットフォーム起因の切断が起きた際、最初からやり直すのではなく、最後に保存されたチェックポイントからレジューム（再開）してリトライするよう改善しました (#977, PR #978; commonlib PR #59)。(@apple-ouyang さん、修正対応ありがとうございます！)
- 処理が中断された「すべてをフェッチ」操作の実行待ち（Pending）状態において、Simple Fetch が以前にユーザーが選択したセットアップの回答内容を保持するようにし、リトライ時に同じ質問が繰り返されないよう改善しました (#977, PR #978)。(@apple-ouyang さん、修正対応ありがとうございます！)
- 内部的なファイル同期が無効になっている場合、`.git` パスなどの非表示ストレージイベントが通常の対象ファイルフィルターに到達しないよう修正し、スキップされる前に不要な非対象ログが出力されないようにしました (commonlib PR #60)。(@apple-ouyang さん、修正対応ありがとうございます！)
- ストレージからファイルが削除された際、ストレージファイルがすでに存在しないことでデータベースの墓標（tombstone）ドキュメントが書き込まれず、オフラインスキャナーによって削除済みファイルが復活してしまうことがある問題を修正しました (commonlib PR #56)。(@cosmic-fire-eng さん、修正対応ありがとうございます！)

### 改善

- ローカルデータベースのメンテナンスコマンドを実行する前に、必要なチャンク設定がある場合は確認を挟んで適用し、前提条件を満たしてから処理を進められるよう改善しました (#980, PR #981)。(@apple-ouyang さん、改善対応ありがとうございます！)
- `octagonal-wheels` の新しい `StreamInbox` ヘルパーを利用し、CouchDB レプリケーションイベント処理を改善しました (commonlib PR #62)。

### ドキュメント

- セットアップドキュメントの目次に `nginx` を追加しました (PR #976)。(@kiraventom さん、改善対応ありがとうございます！)

### その他

- 新しく公開されたヘルパーモジュールを使用するため、プラグインおよびワークスペースのパッケージ全体で `octagonal-wheels` を `0.1.47` に更新しました。

## 0.25.78

2026年6月23日

### 修正

- 高速同期（Fast Fetch）処理中に一部のエラーが発生した場合に、データベース全体の巻き戻しおよび再フェッチが発生する問題を修正しました (#972, PR #973)。(@apple-ouyang さん、修正対応ありがとうございます！)

### 改善

- オブジェクトストレージ（MinIO、S3など）のレプリケーションエンジンを全面的に刷新しました（「ジャーナルレプリケータ第2世代」）。
  - 標準の Web Streams API を活用し、バックプレッシャー（背圧）に対応した堅牢なアーキテクチャに移行。巨大な Vault でのメモリフットプリントや一時ストレージ使用量を削減します。
  - 物理ストレージロジックを分離し、将来的に新しいストレージバックエンドを追加しやすくしました。
  - オブジェクトストレージ使用時において、CouchDB レプリケーションプロトコル（`new_edits: false` を伴う正しい `_revisions` 転送）への準拠をより厳格にしました。

### テスト

- 新しい `JournalSyncCore` エンジン向けに、ストリーム・背圧・`new_edits: false` の検証をカバーする包括的なユニットテストを追加しました。
- 標準の環境変数を使用して MinIO テストを自動実行できるよう、CI パイプラインにおける統合テストのワークフローを改善しました。

## 0.25.77

2026年6月19日

ユーザーにとってはほとんど意味のないアップデートですが、開発メンバー（維持管理人）にとってはそうではないことを祈ります。最初からうまく設計できていれば、こんな手間はかからなかったはずです。本当に良い機会でした。

また、今回は非常に大きなアップデートで、多くの時間を費やし、CI テストも行いましたが、ほとんどは型の修正です。問題を見つけたらすぐにお知らせください！

### 改善

- Obsidian v1.7.2 以降では、プラグイン独自のゴミ箱設定に関わらず、ファイルの削除がユーザーの削除設定（`FileManager.trashFile` API を利用）を尊重するようになりました。

### その他

- ライブラリの型定義（Typings）が含まれるようになりました。
- 多くの型エラーを修正・改善しました。
- インポートパスをルートおよび `lib/src` ディレクトリからの相対パスに正規化し、ライブラリとプラグインの境界を壊さないようにしました。
- CLI や webapp などのサブプロジェクトがワークスペースに配置されるようになりました。

## 0.25.76

2026年6月15日

### 修正

- カスタムヘッダーを使用する S3 接続が正常に動作するよう修正しました (#875)。
  - 以前は、プロキシ認証用に挿入されたカスタムヘッダーが AWS Signature v4 の計算に誤って含まれていました。これにより、厳格な S3 バックエンド（Garage など）や、ストレージサービスに到達する前にリバースプロキシがヘッダーの変更・名前変更・削除を行った環境において、「400 Bad Request」（「署名されたヘッダーが存在しない」など）のエラーが発生していました。
- 特定のプラットフォームにおいて、P2P 同期の接続情報が破損する問題を修正しました (#956)。

## 0.25.75

2026年6月13日

### 修正

- 一部の環境で高速同期（Fast Fetch）を使用すると TypeError が発生する問題を修正しました (#953)。

### 新機能

- デスクトップ環境において、バックグラウンドでレプリケーションを有効に維持する設定を追加しました (#939, PR #949)。(@migsferro さん、本当にありがとうございます！)

### 修正（CLI、自動実行）

- `force` パラメータまたは `writeDocumentsIfConflicted` 設定が有効な場合でも、未同期のローカル変更の上書きを防ぐ競合保護チェックによって `mirror` コマンドがアップデート適用に失敗することがある問題を修正しました。

### 改善

- (CLI) 残りの bash 回帰テスト（`test-daemon-linux.sh`、`test-decoupled-vault-linux.sh`、`test-remote-commands-linux.sh`）を Deno に移植し、クロスプラットフォームで検証できるようにしました。

### その他

- 一部の依存関係を更新しました。
- 依存関係のアップグレード後も古い iOS バージョンで動作することを確認するため、CI テストで iOS 15 との互換性チェックを追加しました。

## 0.25.74

2026年6月8日

### 修正

- 非表示ファイルの同期を無効にしても効果がなく、対象外の非表示ファイルがレプリケーションや起動シーケンスのスキャンによって処理および同期され続ける問題を修正しました (#941)。
- 競合するリビジョンのいずれかが削除されている場合に、競合リビジョンの自動マージを防止しました。これにより削除されたファイルが再出現するのを防ぎます (#911)。
- 起動シーケンスにおいて、より効果的に状態を保存するよう改善しました（@bmcyver さん、本当にありがとうございます）！

## CLI のみ

2026年6月8日

CLI のバージョン管理についても考慮すべきですね……。

### 改善

- 新しいリモートデータベース管理コマンド `remote-status`、`unlock-remote`、`lock-remote`、および `mark-resolved` を追加しました。
- `--vault`（または `-V`）オプションを使用して、データベースディレクトリのパスと実際の Vault ディレクトリのパスを分離（デカップル）できるようにしました。

### 修正（予防的）

- CLI の起動前に、指定された Vault パスが存在し、実際にディレクトリであることを検証するようにしました。
- 単発コマンド（`'push'`、`'pull'`、`'cat'`、`'rm'`、`'info'`、`'resolve'` など）において、データベースパスではなく分離された Vault パスに対するパス解決と検証を統合しました。

## 0.25.73

2026年6月4日

### 修正

- CouchDB のデータベース名チェックを仕様に合わせて調整しました (#926)。
- MinIO および P2P において「このデバイスの同期をリセット（Reset Synchronisation on This Device）」が正常に動作するよう修正しました。

## ~~0.25.71~~ 0.25.72

0.25.71 は必要な修正（オブジェクトストレージ関連）のためキャンセルされました。

2026年6月3日

### 改善

- 初期設定時のデータベース取得（別名：このデバイスでの同期のリセット）がストリーミングをサポートし、より高速になりました（CouchDBのみ）。
- データベース取得プロセスが合理化され、完了するまでデータベース操作が一時停止されるようになりました。
- 初期同期プロセスが簡素化され、リモートサーバーとのファイル同期がより簡単になりました。
- 複数のリモートデータベースが設定されている場合（複数のCouchDBやS3リモートなど）、初期設定時に取得元のリモートデータベースを選択できるようになりました。
- ヘブライ語（he）の翻訳が追加されました（@MusiCode1 さん、本当にありがとうございます）！
- 翻訳の読み込み時間が短縮されました（@bmcyver さん、本当にありがとうございます）！

### 修正

- ステータス要素が他のプラグインの操作を妨げないようになりました（#930）。
- オブジェクトストレージを使用した初期データベース取得中にファイルイベントが発生しなくなりました。

### リファクタリング

- 新しいコミュニティ自動テストをサポートするため、多数のLint警告を修正しました。これにより、潜在的な問題も解決された可能性があります。

## 0.25.70

2026年5月25日

### 新機能

- 差分ダイアログで、差分を移動して理解しやすくするための機能が追加されました。
  - 同一部分を折りたたんで実際の差分に集中しやすくするチェックボックスを追加しました。 (PR #889)
  - 過去リビジョン内の特定テキストを検索し、検索結果をダイアログ内で強調表示しながらリビジョンを移動できるようになりました。 (PR #890)
- 競合解決ダイアログで、競合箇所を移動できるようになりました。 (PR #891)

これらの機能を実装してくださった @SeleiXi さんに感謝します。

### 改善

- P2P 接続について、接続失敗の理由や現在の接続状態など、より多くの診断情報が表示されるようになりました。

## 0.25.69

2026年5月22日

### 修正

- P2P パスフレーズの不一致でサーバーが停止しないようになりました。
- P2P 同期に関する設定が起動時に正しく適用され、元に戻らないようになりました。

### 新機能

- P2P 接続の診断統計を確認できるようになりました。
  - 接続試行、成功、失敗の回数を確認できます。

## 0.25.68

2026年5月22日

### 改善

- P2P 接続を少し改善しました。
  - `trystero` を v0.24.0 に更新し、イベントハンドラーの割り当てを修正しました。これにより、P2P 接続の確立に失敗したり、メッセージが正しく処理されなかったりする一部のエッジケースが修正されるはずです。
  - 一部環境でランタイムエラーを引き起こす可能性がある minify の問題を避けるため、terser オプションを緩和しました。

## ~~0.25.66~~ 0.25.67

2026年5月20日

0.25.66 には、互換性はあるものの損失が生じる可能性のある設定不一致を自動受け入れするロジックが、意図どおり動作しない不具合がありました。

### 新機能

- 互換性のある Tweak 設定の不一致を自動受け入れする設定を実装し、不一致解決ロジックを強化しました。

### 改善

- Tweak 設定の不一致解決に関する多くのメッセージを、より分かりやすく更新しました。

## 0.25.65

2026年5月19日

### 修正

- iOSでバックグラウンドから復帰した際の問題を修正しました。 (#888)
- チャンク分割方式 `V3: Fine Deduplication` が再び正しく動作するようになりました。 (#866)
  - 生成されるチャンク数が少なくなるなどの欠点はありますが、ファイルが完全に入れ替わったのではなく部分的に変更された場合、転送量と保存容量を抑えられます。
- まだ送信されていない未同期のローカル変更が、競合として正しく保持されるようになりました。 @SeleiXi さん、ありがとうございます。
- 現在のリビジョンと競合リビジョンの内容が同一の場合、新しいリビジョンを作成しないようにしました。 @daichi-629 さん、ありがとうございます。

### 改善

- 起動処理中の並行処理エラーについて、詳細な情報が出るようにしました。
- `report` に直近のログが含まれるようになりました。設定で verbose を有効にしていない場合でも、レポートには verbose レベルのログが含まれます。
- 短時間に大量のログが生成された場合の過剰な更新を避けるため、ログ表示の更新をデバウンスしました。
- 設定画面を開かずにレポートを生成できる `Issue作成用のデバッグ情報付き完全レポートを生成` コマンドをコマンドパレットに追加しました。

## 0.25.64

2026年5月17日

### P2P状態ペイン

- 有効なP2Pリモートを選択するコンボボックスと、P2PセットアップダイアログからP2Pリモートを作成・選択する `+` アクションを追加しました。
- 承認済みピアに対して、ピアごとに今すぐレプリケーションを実行できるアクションを追加しました。
- 状態操作アイコンを分かりやすく更新しました。
  - 今すぐレプリケーション: `🔄`（実行中は `⏳`）
  - 監視: `🔔` / `🔕`
  - 同期対象: `🔗` / `⛓️‍💥`
- 有効なP2Pリモートが選択されていない場合の警告状態を追加しました。

### P2P状態カード

- 識別しやすくするため、安定したルームID接尾辞の表示を追加し、ピアIDの上に配置しました。

### 内部変更（動作変更なし）

#### P2P

- 通常の有効リモートとは別に、P2P機能専用の有効リモート選択として `P2P_ActiveRemoteConfigurationId` を追加しました。
- `remoteType` を変更せずにP2P設定を反映する、P2P専用リモート構成の有効化ロジックを追加しました。
- 必要に応じてP2Pの有効リモート選択を引き継ぐマイグレーションを追加しました。
- 共有のルームIDユーティリティ関数を追加し、P2PセットアップとP2Pペイン全体で使用するようにしました。

#### テスト

- P2Pの有効リモート適用に関する設定読み込み動作の単体テストを追加・更新しました。

## 0.25.63

2026年5月17日

### 修正

- P2Pのみのモードで同期できない問題を修正しました。
- P2Pを主なリモート種別としている場合に、redFlagの再構築フローで「リモートサーバーへの接続に失敗しました」と表示される問題を修正しました。P2Pではリモート設定の取得をスキップするようになりました。

### P2PレプリケーションUIの改善

- 接続状態とピアネットワークをリアルタイムに確認できる、新しいP2P状態ペインを追加しました。
  - 新しいP2P機能の詳しい使い方は、更新された [ユーザーガイド: Peer-to-Peer同期（2026年版）](./docs/p2p_sync_updates_2026.md) を参照してください。
- `Replicate` ボタンまたはリボンアイコンから、1クリックで双方向同期を行う新しい対話型レプリケーションダイアログを開くようになりました。
- Vault再構築フロー（`replicateAllFromServer`）では、テキストだけの選択ダイアログではなく、新しいP2Pレプリケーションモーダルを開くようになりました。これにより一貫したUIで操作できます。

## 0.25.62

2026年5月14日

### 修正

- セットアップウィザードや構成チェックを通らずに新しいリモートデータベースへ接続しようとしたとき、接続を確立できない問題を修正しました。 (#660)

## 0.25.61

2026年5月13日

### 改善

- P2P同期の基盤がより堅牢になりました。トランスポート層、シグナリング・接続層、RPC層に分離され、それぞれの単体テストが追加されています。この変更により、P2P同期はRPC化されたPouchDB同期を使う堅牢な仕組みに更新されました。
- このP2P同期は接続面で以前のバージョンと互換性がありません。すべてのデバイスを更新する必要があります。

### 修正

- 初期化の早い段階で設定更新が発生したとき、分かりにくいエラーが発生しなくなりました。
- `NetworkWarningStyle` が `Hidden` の場合、ネットワークエラー通知ポップアップが抑制されるようになりました。 @SeleiXi さん、ありがとうございます。

### 新機能

- 差分ビューに差分ナビゲーションボタンが追加され、差分間を移動しやすくなりました。 @SeleiXi さん、ありがとうございます。 (#871)

## 0.25.60

2026年4月29日

### 修正

- 大きな設定でも QR コード経由で問題なくエクスポート・インポートできるようになりました。 (#595)
  - 設定データが QR コードの容量を超える場合、複数の QR コードに分割されます。
    - これらの QR コードは aggregator ページで再結合されます。aggregator ページは分割データを収集し、元の設定を復元します。
    - Aggregator ページは `https://vrtmrz.github.io/obsidian-livesync/aggregator.html` で利用でき、このファイルもリポジトリに含まれています。
  - 設定データはサーバーへ送信されません。QR コードデータはすべてクライアント側で生成・処理されるため、設定はプライベートかつ安全に保たれます。ただし、ネットワーク環境には注意してください。
- QR コード経由で設定をインポート・エクスポートする際、一部のケースで問題を起こしていた設定のシリアライズ・デシリアライズ時のエラーを修正しました。

### 修正（CLI）

- `ls` と `mirror` コマンドで、ドキュメントが見つからない場合やフィルターによってすべてのファイルがスキップされた場合に、情報が表示されるようになりました。何も表示せず終了していた問題を解消しています。 (#860)
  - 処理対象数の合計を含めることで、CLI コマンドのログを分かりやすくしました。
- コマンドライン引数 `vault` を、より適切な名前である `databaseDir` に変更しました。
- `mirror` コマンドで `vault` ディレクトリを指定できるようになりました。これは実際のファイルが保存される場所を示します。互換性のため、以前の動作も引き続きサポートされます。

## 0.25.59

### 修正

- セットアップウィザードがユーザー名とパスワードを黙って失うことがなくなりました。 (#865)
  - @koteitan さん、ありがとうございます。
- Setup URI が正しくインポートされるようになりました。 (#859)
  - こちらも @koteitan さん、ありがとうございます。

### 改善

- @foXaCe さんによりフランス語翻訳が追加されました。ありがとうございます。

## 0.25.58

### 修正

- オブジェクトストレージ設定中に認証情報が壊れなくなりました。 (related: #852)
- チャンク分割中に `Maximum call stack size exceeded` が発生する可能性があった、ワーカー側の再帰問題を修正しました。 (related: #855)
- バックグラウンドワーカーのクラッシュ後処理を改善し、保留中の分割・暗号化タスクが待機状態のまま残らず、きれいに解放されるようにしました。 (related: #855)
- 起動時に、選択中のリモート構成が実行時の接続フィールドにも適用されるようになりました。これにより、古い実行時設定が原因の断続的な認証失敗が減ります。 (related: #855)
- Issue レポート生成時に `remoteConfigurations` の接続文字列が伏せられ、スキームのみ（例: `sls+https://`）を残すようになりました。レポートに認証情報が露出しません。
- 隠しファイルの JSON 競合で、対応前にマージダイアログが開いては閉じる状態を繰り返さなくなりました。プラグイン設定同期で `data.json` の解決不能な競合が残り続ける問題を修正しています。 (related: #850)

## 0.25.57

2026年4月9日

- Journal Sync でアップロード対象がないバッチでも、バッチ処理が継続するようになりました。
- 初期化の早い段階で、replicator に関する予期しないエラーが出なくなりました。
- エディタ内のステータス表示が無効な場合、エラーメッセージも非表示のまま維持されるようになりました。 (related: #829)
- オブジェクトストレージモードで別デバイスが「Fresh Start Wipe」や「Overwrite remote」を実行した後、他のデバイスがアップロードできなくなる問題を修正しました。 (#848)
  - 各デバイスのローカル重複排除キャッシュ（`knownIDs`, `sentIDs`, `receivedFiles`, `sentFiles`）が、リモートに保存された暗号化パラメータから得られるリモート Journal epoch を追跡するようになりました。
  - epoch が変わると、プラグインはそのデバイスが最後にアップロードしたファイルがリモートにまだ存在するか確認します。ファイルがなければリモートワイプと判断し、古いキャッシュを自動的にクリアします。ファイルがまだ存在する場合（例: ワイプを伴わないプロトコル更新）はキャッシュを維持し、epoch だけを更新します。通常のアップグレードで不要な再処理が発生しなくなります。

### 翻訳

- ロシア語翻訳が追加されました。貢献してくださった @vipka1n さん、ありがとうございます。 (#845)

### 新機能

- 同じ種類のリモートデータベースを複数設定できるようになりました。例えば複数の CouchDB や S3 リモートを設定できます。
  - 複数のリモートデータベースを管理するユーザーインターフェースが設定ダイアログに追加されました。UI は説明不要だと思いますが、不明点があれば知らせてください。
- 設定ダイアログで複数のリモートデータベースを切り替えられるようになりました。

### CLI

#### 修正

- CLI でレプリケーション進捗が正しく保存・復元されるようになりました。 (related: #846)

## ~~0.25.55~~ 0.25.56

2026年3月30日

### 修正

- P2P 同期を有効にしておらず、P2P 同期を使う想定もない場合に、`Peer-to-Peer Sync is not enabled. We cannot open a new connection.` エラーが発生しなくなりました。 (#830)

### CLI

- CLI の localStorage サポートが不完全だった問題を修正しました。 (#831) @rewse さん、ありがとうございます。
- ロックされたことのあるリモートへ CLI から接続できない問題を修正しました。 (#833) こちらも @rewse さんに感謝します。

## 0.25.54

2026年3月18日

### 修正

- リモートストレージサイズのチェックが再び正しく動作するようになりました。 (#818)
- 設定ダイアログの一部ボタンが再び正しく反応するようになりました。 (#827)

### リファクタリング

- P2P replicator を、少し堅牢で理解しやすい形にリファクタリングしました。
- 潜在的な問題の原因になり得る、使われなくなった項目を削除しました。

### CLI

- ヘルプ表示の崩れを修正しました。
- 不要なコードを削除しました。

### WebApp

- ログペインで詳細レベルが適用されていなかった問題を修正しました。
- ポップアップが表示されるようになりました。
- テストのカバレッジを追加しました。
- Web app でもポップアップが表示されるようになりました。

## 0.25.53

2026年3月17日

マイナーバージョンアップにすべきか迷いましたが、実際にテストしたところ互換性は保たれているようだったため、そうしませんでした。

### 修正

#### P2P 同期

- P2P 同期の不安定なタイミング問題を修正しました。
- P2P 操作中（承認待ち）に予期しない `Unhandled Rejections` が発生しなくなりました。

#### Journal Sync

- Journal Sync で一部の競合を解決できない問題を修正しました。
- 安定性と信頼性を高めるため、多数の小さな修正を行いました。

### テスト

- P2P の End-to-End テストを、CLI をホストとして使う形に書き直しました。

### CLI

これまで FileSystem LiveSync や各種コンポーネントは別リポジトリで開発していましたが、更新が大きく遅れ、互換性問題にも悩まされてきました。現在、同じコアロジックを使った CLI ツールが形になりつつあります。これはファイルシステムを直接操作するものではありませんが、より便利な作業方法を提供し、オブジェクトストレージとも通信できます。競合解決も可能です。詳細は `src/apps/cli` の [self-hosted-livesync-cli](./src/apps/cli/README.md) を参照してください。
- ヘッドレス専用版として `self-hosted-livesync-cli` を `src/apps/cli` に追加しました。
- CLI でも P2P 同期とオブジェクトストレージをサポートしました。
  - ついに「1ファイルを取得する」ことができました。
  - 仮想環境向けに [LiveSync PeerServer](https://github.com/vrtmrz/livesync-serverpeer) も不要になりました。CLI がその役割を果たせます。

- CLI でバイナリファイルもサポートされました。

### リファクタリングまたは内部変更

- ServiceFileAccessBase がバイナリファイルの読み取りを正しく扱うようになりました。
- HeadlessAPIService がオンライン状態（常にオンライン）をプラグインへ正しく提供するようになりました。
- 非ワーカー版の bgWorker が一部の関数を正しく扱うようになりました。
- `ObsidianLiveSyncPlugin` を `ObsidianLiveSyncPlugin` と `LiveSyncBaseCore` に分離しました。
- `LiveSyncCore` が `LiveSyncBaseCore` の型指定版を示すようになりました。
- `plugin.xxx` 参照を、対応する service または `core.xxx` 参照へ書き換えました。
- オフライン変更スキャナーとローカルデータベースの準備を分離しました。
- hook を追加する箇所として、processFileEvent と processSynchroniseResult のデフォルト優先度を設定しました。
- ControlService が処理操作の readiness を提供するようになりました。
- DatabaseService が派生クラス上でデータベースオープンオプションを変更できるようになりました。
- `useOfflineScanner`, `useCheckRemoteSize`, `useRedFlagFeatures` を `LiveSyncBaseCore` ではなく `main.ts` から設定するようになりました。
- Storage Access API が Promise を返すようになりました。より制限のあるストレージプラットフォームへの対応を可能にするためです。
- Journal Replicator がレプリケーション完了後に true を返すようになりました。

### R&D

- Self-hosted LiveSync のブラウザ版を開発中です。現時点では公開利用を想定していませんが、いずれテスト可能にしたいと考えています。
- ブラウザ版のコードは `src/apps/webapp` で確認できます。

## 0.25.52

2026年3月9日

言い訳: `I` が多すぎました。
熱がある間はまったく原因が分かりませんでしたが、体調が戻ると 30 秒ほどで問題を見つけました。ご心配をおかけして申し訳ありません。お待ちいただきありがとうございました。
簡単なテストシナリオを実行する仕組みを考えたいと思っています。Obsidian CLI が動くようになった今は、よい機会に思えます。

bus factor を改善するため、ソースコードをもっと整理する必要があります。協力や貢献を歓迎します。

### 修正

- 親ディレクトリが空でない場合に、予期しない削除伝播が起きなくなりました。 (#813)

### 差し戻しの取り消し

- ModuleCheckRemoteSize の差し戻しを取り消しました。service feature として復帰しました。

## 0.25.51

2026年3月7日

### 差し戻し

- 予期しない問題があったため、ModuleRedFlag と ModuleInitializerFile を以前のバージョンに戻しました。 (#813)
    - 将来、よりよい設計とテストで再実装します。

## 0.25.50

2026年3月3日

注: 0.25.49 は、ログが過剰すぎたためスキップされました（認証情報が verbose レベルに記録されており、Issue 報告時に予期せず露出する可能性があると分かりました）。0.25.49 を使用している場合は、修正を受け取るため 0.25.50 へ更新してください。（ログ以外の挙動変更は想定していません）

### 修正

- Global History ペインで削除済みファイルをクリックできない問題を修正しました。
- Diff view がより具体的なクラスを使うようになりました。 (#803)
- 構成不一致のメッセージを、理解しやすくするため少し追加しました。
    - ユーザー自身で修正できることが分かるよう、`When replication is initiated manually via the command palette or ribbon, a dialogue box will open to address this.` と表示されるようになりました。

### リファクタリング

- `ModuleRedFlag` を `serviceFeatures/redFlag` へリファクタリングし、テストも追加しました。
- `ModuleInitializerFile` を `lib/serviceFeatures/offlineScanner` へリファクタリングし、テストも追加しました。

## 0.25.48

2026年3月2日

未確認の不具合を除き、挙動変更はありません。この更新後に予期しない挙動を見つけた場合は報告してください。

### リファクタリング

- 保守性とテスト容易性を高めるため、ストレージ関連の多くの関数をリファクタリングしました。
    - すべてのプラットフォーム固有ロジックが adapter として提供されるようになり、コアロジックはプラットフォーム非依存になりました。
    - コアロジックにはかなり多くのテストが追加され、プラットフォーム固有ロジックも mock adapter でテストされています。

## 0.25.47

2026年2月27日

年度末はまだ終わっていませんが、プラグインに再び取り組む時間ができました。

### 修正とリファクタリング

- ネットワークからチャンクを取得する際の不可解な挙動を修正しました。
    - チャンクマネージャーを、各領域と責務を担当するレイヤー構造にしました。例: `DatabaseWriteLayer`, `DatabaseReadLayer`, `NetworkLayer`, `CacheLayer`, `ArrivalWaitLayer`。
        - すべてのレイヤーにテストが追加されました。
        - これらのレイヤーを管理する `LayeredChunkManager` を実装しました。こちらもテスト済みです。
    - `EntryManager` はほぼ書き直され、テストも追加されました。

- リモートストレージサイズ通知に対して、再び `Never warn` を設定できるようになりました。

### テスト

- 次のテストを追加しました。
    - `ConflictManager`

## 0.25.46

2026年2月26日

### 修正

- プラグインのアンロード時に予期しないエラーが発生しなくなりました。
- Hidden File Sync が selector を尊重するようになりました。
- protocol-handler の登録が、予期しないエラーを起こさず安全に動作するようになりました。

### リファクタリング

- `ModuleCheckRemoteSize` を serviceFeature に移植し、テストも追加しました。
- 不要なものをいくつか削除しました。
- LiveSyncManagers の依存関係を明示しました。
- LiveSyncLocalDB は LiveSyncManagers の責任を持つようになり、managers を依存関係として受け取らないようになりました。
    - managers の循環依存を避け、所有関係を明確にするためです。
- ChangeManager をリファクタリングしました。潜在的な問題があり、何らかの修正も行われたはずです。
- カバレッジ蓄積のため、一部のテストを Deno のテストランナーから Vitest に移植しました。

## 0.25.45

2026年2月25日

最近のリファクタリングの結果、テストを書きやすくなりました。

### リファクタリング

- 対象ファイルかどうかをチェックしていた `ModuleTargetFilter` を serviceFeature に移植しました。
    - テストも追加しました。middleware-style の力です。
- `ModuleObsidianAPI` を削除し、`APIService` と `RemoteService` に実装しました。
- ネットワークオンライン状態は `databaseService.managers.networkManager` ではなく、`APIService` が責任を持つようになりました。

完全なノートは
[updates_old.md](https://github.com/vrtmrz/obsidian-livesync/blob/main/updates_old.md)
にあります。
