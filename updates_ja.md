# 0.25
2025年7月19日以降（0.25.0-beta1 は 2025年7月13日）

0.25 の冒頭ノートは [updates_old.md](https://github.com/vrtmrz/obsidian-livesync/blob/main/updates_old.md) に移動しました。0.25 では多くの更新が入りましたが、幸い互換性は維持されており、破壊的変更は不要でした。十分に安定すれば、次のバージョンは v1.0.0 になる見込みです。あくまで希望ではあります。

## 0.25.79

2026年6月29日

### 修正

- 高速同期（Fast Fetch）で一時的なストリーム切断などの一般的なネットワーク切断やプラットフォーム起因の切断が起きた際、最初からやり直すのではなく、最後に保存されたチェックポイントからレジューム（再開）してリトライするよう改善しました (#977, PR #978; commonlib PR #59)。(@apple-ouyang さん、修正対応ありがとうございます！)
- 処理が中断された「すべてをフェッチ」操作の実行待ち（Pending）状態において、Simple Fetch が以前にユーザーが選択したセットアップの回答内容を保持するようにし、リトライ時に同じ質問が繰り返されないよう改善しました (#977, PR #978)。(@apple-ouyang さん、修正対応ありがとうございます！)
- 内部的なファイル同期が無効になっている場合、`.git` パスなどの非表示ストレージイベントが通常の対象ファイルフィルターに到達しないよう修正し、スキップされる前に不要な非対象ログが出力されないようにしました (commonlib PR #60)。(@apple-ouyang さん、修正対応ありがとうございます！)
- ストレージからファイルが削除された際、ストレージファイルがすでに存在しないことでデータベースの墓標（tombstone）ドキュメントが書き込まれず、オフラインスキャナーによって削除済みファイルが復活してしまうことがある問題を修正しました (commonlib PR #56)。(@cosmic-fire-eng さん、修正対応ありがとうございます！)

### 改善

- ローカルデータベースのメンテナンスコマンドを実行する前に、必要なチャンク設定がある場合は確認を挟んで適用し、前提条件を満たしてから処理を進められるよう改善しました (#980, PR #981)。(@apple-ouyang さん、改善対応ありがとうございます！)
- `octagonal-wheels` の新しい `StreamInbox` ヘルパーを利用し、CouchDB レプリケーションイベント処理を改善しました (commonlib PR #62)。

### ドキュメント

- セットアップドキュメントの目次に `nginx` を追加しました (PR #976)。(@kiraventom さん、改善対応ありがとうございます！)

### その他

- 新しく公開されたヘルパーモジュールを使用するため、プラグインおよびワークスペースのパッケージ全体で `octagonal-wheels` を `0.1.47` に更新しました。

## 0.25.78

2026年6月23日

### 修正

- 高速同期（Fast Fetch）処理中に一部のエラーが発生した場合に、データベース全体の巻き戻しおよび再フェッチが発生する問題を修正しました (#972, PR #973)。(@apple-ouyang さん、修正対応ありがとうございます！)

### 改善

- オブジェクトストレージ（MinIO、S3など）のレプリケーションエンジンを全面的に刷新しました（「ジャーナルレプリケータ第2世代」）。
  - 標準の Web Streams API を活用し、バックプレッシャー（背圧）に対応した堅牢なアーキテクチャに移行。巨大な Vault でのメモリフットプリントや一時ストレージ使用量を削減します。
  - 物理ストレージロジックを分離し、将来的に新しいストレージバックエンドを追加しやすくしました。
  - オブジェクトストレージ使用時において、CouchDB レプリケーションプロトコル（`new_edits: false` を伴う正しい `_revisions` 転送）への準拠をより厳格にしました。

### テスト

- 新しい `JournalSyncCore` エンジン向けに、ストリーム・背圧・`new_edits: false` の検証をカバーする包括的なユニットテストを追加しました。
- 標準の環境変数を使用して MinIO テストを自動実行できるよう、CI パイプラインにおける統合テストのワークフローを改善しました。

## 0.25.77

2026年6月19日

ユーザーにとってはほとんど意味のないアップデートですが、開発メンバー（維持管理人）にとってはそうではないことを祈ります。最初からうまく設計できていれば、こんな手間はかからなかったはずです。本当に良い機会でした。

また、今回は非常に大きなアップデートで、多くの時間を費やし、CI テストも行いましたが、ほとんどは型の修正です。問題を見つけたらすぐにお知らせください！

### 改善

- Obsidian v1.7.2 以降では、プラグイン独自のゴミ箱設定に関わらず、ファイルの削除がユーザーの削除設定（`FileManager.trashFile` API を利用）を尊重するようになりました。

### その他

- ライブラリの型定義（Typings）が含まれるようになりました。
- 多くの型エラーを修正・改善しました。
- インポートパスをルートおよび `lib/src` ディレクトリからの相対パスに正規化し、ライブラリとプラグインの境界を壊さないようにしました。
- CLI や webapp などのサブプロジェクトがワークスペースに配置されるようになりました。

## 0.25.76

2026年6月15日

### 修正

- カスタムヘッダーを使用する S3 接続が正常に動作するよう修正しました (#875)。
  - 以前は、プロキシ認証用に挿入されたカスタムヘッダーが AWS Signature v4 の計算に誤って含まれていました。これにより、厳格な S3 バックエンド（Garage など）や、ストレージサービスに到達する前にリバースプロキシがヘッダーの変更・名前変更・削除を行った環境において、「400 Bad Request」（「署名されたヘッダーが存在しない」など）のエラーが発生していました。
- 特定のプラットフォームにおいて、P2P 同期の接続情報が破損する問題を修正しました (#956)。

## 0.25.75

2026年6月13日

### 修正

- 一部の環境で高速同期（Fast Fetch）を使用すると TypeError が発生する問題を修正しました (#953)。

### 新機能

- デスクトップ環境において、バックグラウンドでレプリケーションを有効に維持する設定を追加しました (#939, PR #949)。(@migsferro さん、本当にありがとうございます！)

### 修正（CLI、自動実行）

- `force` パラメータまたは `writeDocumentsIfConflicted` 設定が有効な場合でも、未同期のローカル変更の上書きを防ぐ競合保護チェックによって `mirror` コマンドがアップデート適用に失敗することがある問題を修正しました。

### 改善

- (CLI) 残りの bash 回帰テスト（`test-daemon-linux.sh`、`test-decoupled-vault-linux.sh`、`test-remote-commands-linux.sh`）を Deno に移植し、クロスプラットフォームで検証できるようにしました。

### その他

- 一部の依存関係を更新しました。
- 依存関係のアップグレード後も古い iOS バージョンで動作することを確認するため、CI テストで iOS 15 との互換性チェックを追加しました。

## 0.25.74

2026年6月8日

### 修正

- 非表示ファイルの同期を無効にしても効果がなく、対象外の非表示ファイルがレプリケーションや起動シーケンスのスキャンによって処理および同期され続ける問題を修正しました (#941)。
- 競合するリビジョンのいずれかが削除されている場合に、競合リビジョンの自動マージを防止しました。これにより削除されたファイルが再出現するのを防ぎます (#911)。
- 起動シーケンスにおいて、より効果的に状態を保存するよう改善しました（@bmcyver さん、本当にありがとうございます）！

## CLI のみ

2026年6月8日

CLI のバージョン管理についても考慮すべきですね……。

### 改善

- 新しいリモートデータベース管理コマンド `remote-status`、`unlock-remote`、`lock-remote`、および `mark-resolved` を追加しました。
- `--vault`（または `-V`）オプションを使用して、データベースディレクトリのパスと実際の Vault ディレクトリのパスを分離（デカップル）できるようにしました。

### 修正（予防的）

- CLI の起動前に、指定された Vault パスが存在し、実際にディレクトリであることを検証するようにしました。
- 単発コマンド（`'push'`、`'pull'`、`'cat'`、`'rm'`、`'info'`、`'resolve'` など）において、データベースパスではなく分離された Vault パスに対するパス解決と検証を統合しました。

## 0.25.73

2026年6月4日

### 修正

- CouchDB のデータベース名チェックを仕様に合わせて調整しました (#926)。
- MinIO および P2P において「このデバイスの同期をリセット（Reset Synchronisation on This Device）」が正常に動作するよう修正しました。

## ~~0.25.71~~ 0.25.72

0.25.71 は必要な修正（オブジェクトストレージ関連）のためキャンセルされました。

2026年6月3日

### 改善

- 初期設定時のデータベース取得（別名：このデバイスでの同期のリセット）がストリーミングをサポートし、より高速になりました（CouchDBのみ）。
- データベース取得プロセスが合理化され、完了するまでデータベース操作が一時停止されるようになりました。
- 初期同期プロセスが簡素化され、リモートサーバーとのファイル同期がより簡単になりました。
- 複数のリモートデータベースが設定されている場合（複数のCouchDBやS3リモートなど）、初期設定時に取得元のリモートデータベースを選択できるようになりました。
- ヘブライ語（he）の翻訳が追加されました（@MusiCode1 さん、本当にありがとうございます）！
- 翻訳の読み込み時間が短縮されました（@bmcyver さん、本当にありがとうございます）！

### 修正

- ステータス要素が他のプラグインの操作を妨げないようになりました（#930）。
- オブジェクトストレージを使用した初期データベース取得中にファイルイベントが発生しなくなりました。

### リファクタリング

- 新しいコミュニティ自動テストをサポートするため、多数のLint警告を修正しました。これにより、潜在的な問題も解決された可能性があります。

## 0.25.70

2026年5月25日

### 新機能

- 差分ダイアログで、差分を移動して理解しやすくするための機能が追加されました。
  - 同一部分を折りたたんで実際の差分に集中しやすくするチェックボックスを追加しました。 (PR #889)
  - 過去リビジョン内の特定テキストを検索し、検索結果をダイアログ内で強調表示しながらリビジョンを移動できるようになりました。 (PR #890)
- 競合解決ダイアログで、競合箇所を移動できるようになりました。 (PR #891)

これらの機能を実装してくださった @SeleiXi さんに感謝します。

### 改善

- P2P 接続について、接続失敗の理由や現在の接続状態など、より多くの診断情報が表示されるようになりました。

## 0.25.69

2026年5月22日

### 修正

- P2P パスフレーズの不一致でサーバーが停止しないようになりました。
- P2P 同期に関する設定が起動時に正しく適用され、元に戻らないようになりました。

### 新機能

- P2P 接続の診断統計を確認できるようになりました。
  - 接続試行、成功、失敗の回数を確認できます。

## 0.25.68

2026年5月22日

### 改善

- P2P 接続を少し改善しました。
  - `trystero` を v0.24.0 に更新し、イベントハンドラーの割り当てを修正しました。これにより、P2P 接続の確立に失敗したり、メッセージが正しく処理されなかったりする一部のエッジケースが修正されるはずです。
  - 一部環境でランタイムエラーを引き起こす可能性がある minify の問題を避けるため、terser オプションを緩和しました。

## ~~0.25.66~~ 0.25.67

2026年5月20日

0.25.66 には、互換性はあるものの損失が生じる可能性のある設定不一致を自動受け入れするロジックが、意図どおり動作しない不具合がありました。

### 新機能

- 互換性のある Tweak 設定の不一致を自動受け入れする設定を実装し、不一致解決ロジックを強化しました。

### 改善

- Tweak 設定の不一致解決に関する多くのメッセージを、より分かりやすく更新しました。

## 0.25.65

2026年5月19日

### 修正

- iOSでバックグラウンドから復帰した際の問題を修正しました。 (#888)
- チャンク分割方式 `V3: Fine Deduplication` が再び正しく動作するようになりました。 (#866)
  - 生成されるチャンク数が少なくなるなどの欠点はありますが、ファイルが完全に入れ替わったのではなく部分的に変更された場合、転送量と保存容量を抑えられます。
- まだ送信されていない未同期のローカル変更が、競合として正しく保持されるようになりました。 @SeleiXi さん、ありがとうございます。
- 現在のリビジョンと競合リビジョンの内容が同一の場合、新しいリビジョンを作成しないようにしました。 @daichi-629 さん、ありがとうございます。

### 改善

- 起動処理中の並行処理エラーについて、詳細な情報が出るようにしました。
- `report` に直近のログが含まれるようになりました。設定で verbose を有効にしていない場合でも、レポートには verbose レベルのログが含まれます。
- 短時間に大量のログが生成された場合の過剰な更新を避けるため、ログ表示の更新をデバウンスしました。
- 設定画面を開かずにレポートを生成できる `Issue作成用のデバッグ情報付き完全レポートを生成` コマンドをコマンドパレットに追加しました。

## 0.25.64

2026年5月17日

### P2P状態ペイン

- 有効なP2Pリモートを選択するコンボボックスと、P2PセットアップダイアログからP2Pリモートを作成・選択する `+` アクションを追加しました。
- 承認済みピアに対して、ピアごとに今すぐレプリケーションを実行できるアクションを追加しました。
- 状態操作アイコンを分かりやすく更新しました。
  - 今すぐレプリケーション: `🔄`（実行中は `⏳`）
  - 監視: `🔔` / `🔕`
  - 同期対象: `🔗` / `⛓️‍💥`
- 有効なP2Pリモートが選択されていない場合の警告状態を追加しました。

### P2P状態カード

- 識別しやすくするため、安定したルームID接尾辞の表示を追加し、ピアIDの上に配置しました。

### 内部変更（動作変更なし）

#### P2P

- 通常の有効リモートとは別に、P2P機能専用の有効リモート選択として `P2P_ActiveRemoteConfigurationId` を追加しました。
- `remoteType` を変更せずにP2P設定を反映する、P2P専用リモート構成の有効化ロジックを追加しました。
- 必要に応じてP2Pの有効リモート選択を引き継ぐマイグレーションを追加しました。
- 共有のルームIDユーティリティ関数を追加し、P2PセットアップとP2Pペイン全体で使用するようにしました。

#### テスト

- P2Pの有効リモート適用に関する設定読み込み動作の単体テストを追加・更新しました。

## 0.25.63

2026年5月17日

### 修正

- P2Pのみのモードで同期できない問題を修正しました。
- P2Pを主なリモート種別としている場合に、redFlagの再構築フローで「リモートサーバーへの接続に失敗しました」と表示される問題を修正しました。P2Pではリモート設定の取得をスキップするようになりました。

### P2PレプリケーションUIの改善

- 接続状態とピアネットワークをリアルタイムに確認できる、新しいP2P状態ペインを追加しました。
  - 新しいP2P機能の詳しい使い方は、更新された [ユーザーガイド: Peer-to-Peer同期（2026年版）](./docs/p2p_sync_updates_2026.md) を参照してください。
- `Replicate` ボタンまたはリボンアイコンから、1クリックで双方向同期を行う新しい対話型レプリケーションダイアログを開くようになりました。
- Vault再構築フロー（`replicateAllFromServer`）では、テキストだけの選択ダイアログではなく、新しいP2Pレプリケーションモーダルを開くようになりました。これにより一貫したUIで操作できます。

## 0.25.62

2026年5月14日

### 修正

- セットアップウィザードや構成チェックを通らずに新しいリモートデータベースへ接続しようとしたとき、接続を確立できない問題を修正しました。 (#660)

## 0.25.61

2026年5月13日

### 改善

- P2P同期の基盤がより堅牢になりました。トランスポート層、シグナリング・接続層、RPC層に分離され、それぞれの単体テストが追加されています。この変更により、P2P同期はRPC化されたPouchDB同期を使う堅牢な仕組みに更新されました。
- このP2P同期は接続面で以前のバージョンと互換性がありません。すべてのデバイスを更新する必要があります。

### 修正

- 初期化の早い段階で設定更新が発生したとき、分かりにくいエラーが発生しなくなりました。
- `NetworkWarningStyle` が `Hidden` の場合、ネットワークエラー通知ポップアップが抑制されるようになりました。 @SeleiXi さん、ありがとうございます。

### 新機能

- 差分ビューに差分ナビゲーションボタンが追加され、差分間を移動しやすくなりました。 @SeleiXi さん、ありがとうございます。 (#871)

## 0.25.60

2026年4月29日

### 修正

- 大きな設定でも QR コード経由で問題なくエクスポート・インポートできるようになりました。 (#595)
  - 設定データが QR コードの容量を超える場合、複数の QR コードに分割されます。
    - これらの QR コードは aggregator ページで再結合されます。aggregator ページは分割データを収集し、元の設定を復元します。
    - Aggregator ページは `https://vrtmrz.github.io/obsidian-livesync/aggregator.html` で利用でき、このファイルもリポジトリに含まれています。
  - 設定データはサーバーへ送信されません。QR コードデータはすべてクライアント側で生成・処理されるため、設定はプライベートかつ安全に保たれます。ただし、ネットワーク環境には注意してください。
- QR コード経由で設定をインポート・エクスポートする際、一部のケースで問題を起こしていた設定のシリアライズ・デシリアライズ時のエラーを修正しました。

### 修正（CLI）

- `ls` と `mirror` コマンドで、ドキュメントが見つからない場合やフィルターによってすべてのファイルがスキップされた場合に、情報が表示されるようになりました。何も表示せず終了していた問題を解消しています。 (#860)
  - 処理対象数の合計を含めることで、CLI コマンドのログを分かりやすくしました。
- コマンドライン引数 `vault` を、より適切な名前である `databaseDir` に変更しました。
- `mirror` コマンドで `vault` ディレクトリを指定できるようになりました。これは実際のファイルが保存される場所を示します。互換性のため、以前の動作も引き続きサポートされます。

## 0.25.59

### 修正

- セットアップウィザードがユーザー名とパスワードを黙って失うことがなくなりました。 (#865)
  - @koteitan さん、ありがとうございます。
- Setup URI が正しくインポートされるようになりました。 (#859)
  - こちらも @koteitan さん、ありがとうございます。

### 改善

- @foXaCe さんによりフランス語翻訳が追加されました。ありがとうございます。

## 0.25.58

### 修正

- オブジェクトストレージ設定中に認証情報が壊れなくなりました。 (related: #852)
- チャンク分割中に `Maximum call stack size exceeded` が発生する可能性があった、ワーカー側の再帰問題を修正しました。 (related: #855)
- バックグラウンドワーカーのクラッシュ後処理を改善し、保留中の分割・暗号化タスクが待機状態のまま残らず、きれいに解放されるようにしました。 (related: #855)
- 起動時に、選択中のリモート構成が実行時の接続フィールドにも適用されるようになりました。これにより、古い実行時設定が原因の断続的な認証失敗が減ります。 (related: #855)
- Issue レポート生成時に `remoteConfigurations` の接続文字列が伏せられ、スキームのみ（例: `sls+https://`）を残すようになりました。レポートに認証情報が露出しません。
- 隠しファイルの JSON 競合で、対応前にマージダイアログが開いては閉じる状態を繰り返さなくなりました。プラグイン設定同期で `data.json` の解決不能な競合が残り続ける問題を修正しています。 (related: #850)

## 0.25.57

2026年4月9日

- Journal Sync でアップロード対象がないバッチでも、バッチ処理が継続するようになりました。
- 初期化の早い段階で、replicator に関する予期しないエラーが出なくなりました。
- エディタ内のステータス表示が無効な場合、エラーメッセージも非表示のまま維持されるようになりました。 (related: #829)
- オブジェクトストレージモードで別デバイスが「Fresh Start Wipe」や「Overwrite remote」を実行した後、他のデバイスがアップロードできなくなる問題を修正しました。 (#848)
  - 各デバイスのローカル重複排除キャッシュ（`knownIDs`, `sentIDs`, `receivedFiles`, `sentFiles`）が、リモートに保存された暗号化パラメータから得られるリモート Journal epoch を追跡するようになりました。
  - epoch が変わると、プラグインはそのデバイスが最後にアップロードしたファイルがリモートにまだ存在するか確認します。ファイルがなければリモートワイプと判断し、古いキャッシュを自動的にクリアします。ファイルがまだ存在する場合（例: ワイプを伴わないプロトコル更新）はキャッシュを維持し、epoch だけを更新します。通常のアップグレードで不要な再処理が発生しなくなります。

### 翻訳

- ロシア語翻訳が追加されました。貢献してくださった @vipka1n さん、ありがとうございます。 (#845)

### 新機能

- 同じ種類のリモートデータベースを複数設定できるようになりました。例えば複数の CouchDB や S3 リモートを設定できます。
  - 複数のリモートデータベースを管理するユーザーインターフェースが設定ダイアログに追加されました。UI は説明不要だと思いますが、不明点があれば知らせてください。
- 設定ダイアログで複数のリモートデータベースを切り替えられるようになりました。

### CLI

#### 修正

- CLI でレプリケーション進捗が正しく保存・復元されるようになりました。 (related: #846)

## ~~0.25.55~~ 0.25.56

2026年3月30日

### 修正

- P2P 同期を有効にしておらず、P2P 同期を使う想定もない場合に、`Peer-to-Peer Sync is not enabled. We cannot open a new connection.` エラーが発生しなくなりました。 (#830)

### CLI

- CLI の localStorage サポートが不完全だった問題を修正しました。 (#831) @rewse さん、ありがとうございます。
- ロックされたことのあるリモートへ CLI から接続できない問題を修正しました。 (#833) こちらも @rewse さんに感謝します。

## 0.25.54

2026年3月18日

### 修正

- リモートストレージサイズのチェックが再び正しく動作するようになりました。 (#818)
- 設定ダイアログの一部ボタンが再び正しく反応するようになりました。 (#827)

### リファクタリング

- P2P replicator を、少し堅牢で理解しやすい形にリファクタリングしました。
- 潜在的な問題の原因になり得る、使われなくなった項目を削除しました。

### CLI

- ヘルプ表示の崩れを修正しました。
- 不要なコードを削除しました。

### WebApp

- ログペインで詳細レベルが適用されていなかった問題を修正しました。
- ポップアップが表示されるようになりました。
- テストのカバレッジを追加しました。
- Web app でもポップアップが表示されるようになりました。

## 0.25.53

2026年3月17日

マイナーバージョンアップにすべきか迷いましたが、実際にテストしたところ互換性は保たれているようだったため、そうしませんでした。

### 修正

#### P2P 同期

- P2P 同期の不安定なタイミング問題を修正しました。
- P2P 操作中（承認待ち）に予期しない `Unhandled Rejections` が発生しなくなりました。

#### Journal Sync

- Journal Sync で一部の競合を解決できない問題を修正しました。
- 安定性と信頼性を高めるため、多数の小さな修正を行いました。

### テスト

- P2P の End-to-End テストを、CLI をホストとして使う形に書き直しました。

### CLI

これまで FileSystem LiveSync や各種コンポーネントは別リポジトリで開発していましたが、更新が大きく遅れ、互換性問題にも悩まされてきました。現在、同じコアロジックを使った CLI ツールが形になりつつあります。これはファイルシステムを直接操作するものではありませんが、より便利な作業方法を提供し、オブジェクトストレージとも通信できます。競合解決も可能です。詳細は `src/apps/cli` の [self-hosted-livesync-cli](./src/apps/cli/README.md) を参照してください。
- ヘッドレス専用版として `self-hosted-livesync-cli` を `src/apps/cli` に追加しました。
- CLI でも P2P 同期とオブジェクトストレージをサポートしました。
  - ついに「1ファイルを取得する」ことができました。
  - 仮想環境向けに [LiveSync PeerServer](https://github.com/vrtmrz/livesync-serverpeer) も不要になりました。CLI がその役割を果たせます。

- CLI でバイナリファイルもサポートされました。

### リファクタリングまたは内部変更

- ServiceFileAccessBase がバイナリファイルの読み取りを正しく扱うようになりました。
- HeadlessAPIService がオンライン状態（常にオンライン）をプラグインへ正しく提供するようになりました。
- 非ワーカー版の bgWorker が一部の関数を正しく扱うようになりました。
- `ObsidianLiveSyncPlugin` を `ObsidianLiveSyncPlugin` と `LiveSyncBaseCore` に分離しました。
- `LiveSyncCore` が `LiveSyncBaseCore` の型指定版を示すようになりました。
- `plugin.xxx` 参照を、対応する service または `core.xxx` 参照へ書き換えました。
- オフライン変更スキャナーとローカルデータベースの準備を分離しました。
- hook を追加する箇所として、processFileEvent と processSynchroniseResult のデフォルト優先度を設定しました。
- ControlService が処理操作の readiness を提供するようになりました。
- DatabaseService が派生クラス上でデータベースオープンオプションを変更できるようになりました。
- `useOfflineScanner`, `useCheckRemoteSize`, `useRedFlagFeatures` を `LiveSyncBaseCore` ではなく `main.ts` から設定するようになりました。
- Storage Access API が Promise を返すようになりました。より制限のあるストレージプラットフォームへの対応を可能にするためです。
- Journal Replicator がレプリケーション完了後に true を返すようになりました。

### R&D

- Self-hosted LiveSync のブラウザ版を開発中です。現時点では公開利用を想定していませんが、いずれテスト可能にしたいと考えています。
- ブラウザ版のコードは `src/apps/webapp` で確認できます。

## 0.25.52

2026年3月9日

言い訳: `I` が多すぎました。
熱がある間はまったく原因が分かりませんでしたが、体調が戻ると 30 秒ほどで問題を見つけました。ご心配をおかけして申し訳ありません。お待ちいただきありがとうございました。
簡単なテストシナリオを実行する仕組みを考えたいと思っています。Obsidian CLI が動くようになった今は、よい機会に思えます。

bus factor を改善するため、ソースコードをもっと整理する必要があります。協力や貢献を歓迎します。

### 修正

- 親ディレクトリが空でない場合に、予期しない削除伝播が起きなくなりました。 (#813)

### 差し戻しの取り消し

- ModuleCheckRemoteSize の差し戻しを取り消しました。service feature として復帰しました。

## 0.25.51

2026年3月7日

### 差し戻し

- 予期しない問題があったため、ModuleRedFlag と ModuleInitializerFile を以前のバージョンに戻しました。 (#813)
    - 将来、よりよい設計とテストで再実装します。

## 0.25.50

2026年3月3日

注: 0.25.49 は、ログが過剰すぎたためスキップされました（認証情報が verbose レベルに記録されており、Issue 報告時に予期せず露出する可能性があると分かりました）。0.25.49 を使用している場合は、修正を受け取るため 0.25.50 へ更新してください。（ログ以外の挙動変更は想定していません）

### 修正

- Global History ペインで削除済みファイルをクリックできない問題を修正しました。
- Diff view がより具体的なクラスを使うようになりました。 (#803)
- 構成不一致のメッセージを、理解しやすくするため少し追加しました。
    - ユーザー自身で修正できることが分かるよう、`When replication is initiated manually via the command palette or ribbon, a dialogue box will open to address this.` と表示されるようになりました。

### リファクタリング

- `ModuleRedFlag` を `serviceFeatures/redFlag` へリファクタリングし、テストも追加しました。
- `ModuleInitializerFile` を `lib/serviceFeatures/offlineScanner` へリファクタリングし、テストも追加しました。

## 0.25.48

2026年3月2日

未確認の不具合を除き、挙動変更はありません。この更新後に予期しない挙動を見つけた場合は報告してください。

### リファクタリング

- 保守性とテスト容易性を高めるため、ストレージ関連の多くの関数をリファクタリングしました。
    - すべてのプラットフォーム固有ロジックが adapter として提供されるようになり、コアロジックはプラットフォーム非依存になりました。
    - コアロジックにはかなり多くのテストが追加され、プラットフォーム固有ロジックも mock adapter でテストされています。

## 0.25.47

2026年2月27日

年度末はまだ終わっていませんが、プラグインに再び取り組む時間ができました。

### 修正とリファクタリング

- ネットワークからチャンクを取得する際の不可解な挙動を修正しました。
    - チャンクマネージャーを、各領域と責務を担当するレイヤー構造にしました。例: `DatabaseWriteLayer`, `DatabaseReadLayer`, `NetworkLayer`, `CacheLayer`, `ArrivalWaitLayer`。
        - すべてのレイヤーにテストが追加されました。
        - これらのレイヤーを管理する `LayeredChunkManager` を実装しました。こちらもテスト済みです。
    - `EntryManager` はほぼ書き直され、テストも追加されました。

- リモートストレージサイズ通知に対して、再び `Never warn` を設定できるようになりました。

### テスト

- 次のテストを追加しました。
    - `ConflictManager`

## 0.25.46

2026年2月26日

### 修正

- プラグインのアンロード時に予期しないエラーが発生しなくなりました。
- Hidden File Sync が selector を尊重するようになりました。
- protocol-handler の登録が、予期しないエラーを起こさず安全に動作するようになりました。

### リファクタリング

- `ModuleCheckRemoteSize` を serviceFeature に移植し、テストも追加しました。
- 不要なものをいくつか削除しました。
- LiveSyncManagers の依存関係を明示しました。
- LiveSyncLocalDB は LiveSyncManagers の責任を持つようになり、managers を依存関係として受け取らないようになりました。
    - managers の循環依存を避け、所有関係を明確にするためです。
- ChangeManager をリファクタリングしました。潜在的な問題があり、何らかの修正も行われたはずです。
- カバレッジ蓄積のため、一部のテストを Deno のテストランナーから Vitest に移植しました。

## 0.25.45

2026年2月25日

最近のリファクタリングの結果、テストを書きやすくなりました。

### リファクタリング

- 対象ファイルかどうかをチェックしていた `ModuleTargetFilter` を serviceFeature に移植しました。
    - テストも追加しました。middleware-style の力です。
- `ModuleObsidianAPI` を削除し、`APIService` と `RemoteService` に実装しました。
- ネットワークオンライン状態は `databaseService.managers.networkManager` ではなく、`APIService` が責任を持つようになりました。

完全なノートは
[updates_old.md](https://github.com/vrtmrz/obsidian-livesync/blob/main/updates_old.md)
にあります。
